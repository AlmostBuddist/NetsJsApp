module.exports = {
  extends: [
    '@a2seven/eslint-config',
  ],
  rules: {
    /*  delete this rule after update
     * @a2seven/eslint-config package
     * to upper version without this rule
     * */
    'max-lines-per-function': 'off',
  }
};
